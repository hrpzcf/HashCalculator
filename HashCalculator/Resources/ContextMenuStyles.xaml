<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:o="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">

    <!--
        基于 wpfui 默认 ContextMenu 样式（UiContextMenu），仅重写 Template：
        阴影参数沿用 wpfui 原值（BlurRadius=20、ShadowDepth=10、Direction=270、
        Opacity=0.135、Color=#202020）。
        阴影改为冻结的资源（o:Freeze），并去掉 EffectThicknessDecorator，
        根元素直接用 Border（Margin=32）引用冻结的 Effect，避免非 100% DPI 下
        因装饰器非整数 Margin 导致的内容模糊。32 是 4 的倍数，在 125%/150%/175%/200%
        缩放下均为整数物理像素，且大于阴影外扩需求（约 20），不会裁剪阴影。
        
        Border 的 Margin=32 会导致菜单内容相对鼠标锚点向右下偏移，故通过
        ContextMenu 自身的 VerticalOffset / HorizontalOffset 反向补偿定位，
        使菜单左上角回到鼠标位置（ContextMenu 定义了这两个属性，行为同 Popup）。
    -->
    <DropShadowEffect
        x:Key="ContextMenuDropShadow"
        o:Freeze="True"
        BlurRadius="20"
        Direction="270"
        Opacity="0.135"
        ShadowDepth="10"
        Color="#202020" />

    <Style
        x:Key="FixedUiContextMenu"
        BasedOn="{StaticResource UiContextMenu}"
        TargetType="{x:Type ContextMenu}">
        <Setter Property="VerticalOffset" Value="-32" />
        <Setter Property="HorizontalOffset" Value="-34" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border
                        x:Name="Border"
                        Margin="32"
                        Padding="0,3,0,3"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Effect="{StaticResource ContextMenuDropShadow}"
                        SnapsToDevicePixels="True">
                        <Border.RenderTransform>
                            <TranslateTransform />
                        </Border.RenderTransform>
                        <StackPanel
                            ClipToBounds="True"
                            IsItemsHost="True"
                            KeyboardNavigation.DirectionalNavigation="Cycle"
                            Orientation="Vertical" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsOpen" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Border"
                                            Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                            From="-90"
                                            To="0"
                                            Duration="00:00:00.167">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  覆盖默认 ContextMenu 样式，使其应用上述修复  -->
    <Style BasedOn="{StaticResource FixedUiContextMenu}" TargetType="{x:Type ContextMenu}" />

</ResourceDictionary>
