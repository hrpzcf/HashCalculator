<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:o="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml">

    <!--
        基于 wpfui 默认 Flyout 样式，仅重写 Template 为 Flyout 增加阴影。
        阴影参数沿用之前紧凑型参数（BlurRadius=12、Opacity=0.35）。
        阴影改为冻结的资源（o:Freeze），并去掉 EffectThicknessDecorator，
        根元素直接用 Border 引用冻结的 Effect，避免非 100% DPI 下因装饰器
        非整数 Margin 导致的内容模糊。
    -->
    <DropShadowEffect
        x:Key="FlyoutDropShadow"
        o:Freeze="True"
        BlurRadius="12"
        Direction="0"
        Opacity="0.35"
        ShadowDepth="0"
        Color="#202020" />

    <Style BasedOn="{StaticResource DefaultFlyoutStyle}" TargetType="{x:Type wpfui:Flyout}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type wpfui:Flyout}">
                    <Grid>
                        <Popup
                            x:Name="PART_Popup"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                            AllowsTransparency="{TemplateBinding Popup.AllowsTransparency}"
                            Focusable="False"
                            IsOpen="{TemplateBinding IsOpen}"
                            Placement="{TemplateBinding Placement}"
                            PopupAnimation="{TemplateBinding Popup.PopupAnimation}"
                            StaysOpen="{TemplateBinding Popup.StaysOpen}"
                            VerticalOffset="1">
                            <Border
                                x:Name="PopupBorder"
                                Margin="16"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="{DynamicResource PopupCornerRadius}"
                                Effect="{StaticResource FlyoutDropShadow}"
                                SnapsToDevicePixels="True">
                                <ContentPresenter Content="{TemplateBinding Content}" />
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
